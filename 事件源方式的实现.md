# 事件源方式的实现

事件源的描述见[此处](https://github.com/rosy233333/weekly-progress/blob/dev/26.3.9~26.3.15/%E8%B0%83%E5%BA%A6%E5%99%A8%E6%A8%A1%E5%9D%97%E8%AE%BE%E8%AE%A1%E6%96%87%E6%A1%A3.md#%E8%B0%83%E5%BA%A6%E5%99%A8)。

## 2026.5.24讨论结果

### 优先级的修改

统一以接收方任务优先级为标准，每条队列中消息的优先级相同（通道优先级）。

- 对于请求队列，在建立连接时确定通道优先级（也就是接收方任务优先级），因此整个队列中消息的优先级相同
- 对于响应队列，也以接收方（请求方）任务的优先级为标准吗？不同请求方任务可能具有不同的优先级，因此可以分为多条队列，每条队列对应一个优先级的任务？

### 队列设计原本就能减少唤醒事件

只有在接收方阻塞时，才需要发送唤醒事件；如果接收方正在处理其它消息，就只要将消息放入队列即可，无需唤醒。

- 对于请求队列，由于使用一个任务处理所有的请求，因此确实通过上文的机制减少了唤醒事件。
- 对于响应队列，每个响应都对应不同的任务，这些任务在发送请求后即阻塞。因此：
  - 若没有分发机制，则需要对每个响应对应的任务都发送一次唤醒事件
  - 如果有分发机制（分发任务处理所有响应并分发到每个发送请求的任务），则能够减少对分发任务的唤醒，但分发任务仍需要唤醒每个发送请求的任务（不过与其它情况为跨进程唤醒不同，由分发任务进行的是进程内唤醒）。

### 是否需要整合IPC模块和调度模块？

无论是否整合IPC模块和调度模块，都可以采用如下的设计：

- 每条队列一个优先级
- IPC使用一条（或多条不同优先级的）请求队列和多条不同优先级的响应队列

不整合的优势；

- 降低了耦合，降低复杂度，提高可维护性。

整合的优势：

- 若不整合，则除了发送消息以外，还会产生额外的控制流开销；而整合则可以完全消除以下开销：
  - 请求队列：偶尔发送唤醒事件
  - 响应队列：
    - 若不使用分发任务：经常发送唤醒事件
    - 若使用分发任务：偶尔发送唤醒事件；分发任务的开销；分发任务执行的进程内唤醒

（实际上，我设计事件源就是为了降低这一部分控制流开销。）

## 简介

队列可分为两种：请求队列与响应队列。

- 请求队列：使用单个任务处理所有队列项
- 响应队列：不同队列项对应不同任务

由于事件源的要求，需要考虑优先级。

当前的设计以请求者任务的优先级作为其请求消息的优先级；响应消息与请求消息优先级相同，即响应消息的优先级为接收该消息的任务的优先级。如果接收到了某个消息，且已有任务阻塞在该消息上，则该消息的优先级会被计入事件源的最高优先级中；否则不计入。

### 数据流

使用`vqueue`实现。

- 请求队列：为每个优先级的消息分配一条队列。
- 响应队列：为每个消息类型（也就是每个发起请求的任务）分配一条队列。
  - 难点：队列的动态分配；是否可以创建这么多的队列。

### 控制流

- 阻塞与唤醒 -> 队列项对应到协程，通过`wait_queue`暂存阻塞的协程；通过事件源的机制实现唤醒
- 通知 -> 及时更新事件源的优先级；检测到优先级更高时主动让出

根据数据流的设计，无论是请求队列还是响应队列，每条队列的优先级都是固定的。因此，优先级只取决于每条队列的优先级是否被计入最高优先级中。

#### 优先级的更新

优先级变化的情况：

- 接收到消息：如果已有阻塞等待该消息的协程，则将消息的优先级计入最高优先级中。
- 协程阻塞到队列上：如果队列上已有待处理的消息，则将消息的优先级计入最高优先级中。
  - 此处可以设计为只有没有待处理的消息时，协程才会阻塞；这样，协程阻塞到队列上时，一定不会更新优先级。
- 唤醒协程并处理消息：如果队列上不再有其它协程，则不再将消息的优先级计入最高优先级中。

可以使用位图的方式暂存优先级信息。