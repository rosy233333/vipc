# 发送接收流程

## 接收

1. 分配notify_id，若分配到了
2. 从immediate_values中获取，若没获取到
3. 从IPC队列中获取（其中如果获取到了别的协程的消息，则执行“分配”）
4. 将notify_id与id的对应关系插入map中
5. 注册信号处理函数，等待信号或其它协程唤醒
6. 若为信号唤醒，则从IPC队列中获取；若为其它协程唤醒，则从immediate_values中获取，若在对应位置获取不到则出错。

## 分配

1. 将消息放入immediate_values中
2. 唤醒对应的协程

## 发送

1. 向对方的IPC队列中放入通知
2. 从对方的map中查询notify_id
3. 向notify_id发送信号

## 存在的同步问题

### 1. 接收方注册信号处理函数晚于发送方发送信号，导致信号未被捕获，表现为程序退出。

接收4 -> 发送2 -> 发送3 -> 接收5

### 2. 在循环接收情景中，发送方在接收方没有注册信号处理函数的时机发送完毕，导致接收方无法唤醒

接收6 -> 发送1 -> 发送3 -> 接收5

### 3. 在接收过程被信号唤醒后，IPC消息也可能被其它协程放入了immediate_values，导致获取出错

接收(1)5 -> 发送3 -> 接收(2)3 -> 接收(1)6

## 修改方案

1. [x] 修改通知模块语义，从分配notify_id即开始接收对应id的通知，之后调用等待函数时，若有已缓存的通知可以直接返回。在id取消分配时，清除缓存的通知。
2. [ ] 提供专用接口支持“连续接收”，在多次接收间不再释放和重新申请notify_id
3. [ ] 修改接收端获取通知的方式，从信号唤醒后，会检查immediate_values和队列中的通知。（不使用`JoinFuture`，而是直接在一个大Future里写完）

## 修改结果

进行修改1后，在发送方使用信号、接收方不使用信号的情形下，问题1已解决，问题3仍出现。

对修改2进行了调整，不再新增接口，而是使已有接口支持连续接收。、

未实行修改3，而是将使用default_dispatcher的`recv`统一改成从immediate_values中获取、由dispatcher唤醒。

进行如上的修改后，问题均已解决，运行正常。